import * as fromGame from './game-session-store';
import * as fromNavigation from './game-navigation-store';
import * as fromQuestions from './game-question-store';
import * as fromRounds from './game-round-store';

export const game = fromGame;
export const navigation = fromNavigation;
export const questions = fromQuestions;
export const rounds = fromRounds;
