import { NavigationBarMenuDirective } from './navigation-bar-menu.directive';

describe('NavigationBarMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationBarMenuDirective();
    expect(directive).toBeTruthy();
  });
});
