import { NavigationBarTitleDirective } from './navigation-bar-title.directive';

describe('NavigationBarTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationBarTitleDirective();
    expect(directive).toBeTruthy();
  });
});
